spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_plus_finally?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  #查看MySQL的日志信息
  # mapper-locations: classpath:xml/*.xml  # 如果是其他目录，需要配置mapper-locations
#  global-config:
#    db-config:
#      id-type: assign_id  # 配置所有的主键ID都为雪花算法 全局配置